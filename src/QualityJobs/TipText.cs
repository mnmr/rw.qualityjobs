using UnityEngine;

namespace QualityJobs
{
    /// Tooltip text conventions: labels/hints/meta use DimColor (gray).
    /// (Ported from EPrimeReadouts; keep in lockstep.)
    internal static class TipText
    {
        internal static readonly Color DimColor = new Color(0.62f, 0.62f, 0.62f);
    }
}
